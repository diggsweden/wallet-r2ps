// Inbound ports are defined by the application layer use cases themselves.
// The HTTP handlers call into those use cases directly.
// This module is reserved for future command/query trait definitions if needed.
