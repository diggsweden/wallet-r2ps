# SPDX-FileCopyrightText: 2025 diggsweden/wallet-r2ps
#
# SPDX-License-Identifier: CC0-1.0

name: OpenSSF Scorecard Analysis

on:
  push:
    branches:
      - main
  schedule:
    # Saturdays at 02:40 UTC
    - cron: "40 2 * * 6"
    # Wednesdays at 02:40 UTC
    - cron: "40 2 * * 3"
  workflow_dispatch:

permissions:
  contents: read # Best Security practice. Jobs only get read as base, and then permissions are added as needed

jobs:
  scorecard-analysis:
    permissions:
      contents: read
      security-events: write
      id-token: write
    uses: diggsweden/reusable-ci/.github/workflows/security-openssf-scorecard.yml@1e3141deda57eba0e4a9e122b015862c738a2dfb # v2.7.8
    with:
      publish-results: true
